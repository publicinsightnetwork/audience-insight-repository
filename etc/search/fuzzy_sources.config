# options common to all indexes
IncludeConfigFile shared.config

# fields specific to sources index
IncludeConfigFile sources-fields.config

# stemming on (not on in sources.config)
FuzzyIndexingMode Stemming_en1

# turn on XInclude support for additional fuzziness
FollowXInclude yes
